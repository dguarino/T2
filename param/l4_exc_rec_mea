{
    "1" : {
      'component' :  'mozaik.sheets.population_selector.RCRandomN',
      'variables' : ("spikes", "v", "gsyn_exc", "gsyn_inh"),
      'params' :  {
                    'num_of_cells' : 10,
                  }
    },

    ## MANY SPIKES
    #"3" : {
    #  'component' :  'mozaik.sheets.population_selector.RCGrid',
    #  'variables' : ("spikes"),
    #  'params' :  {
    #                'size': 6000.0, # the size of the grid (it is assumed to be square) - it has to be factor of spacing (micro meters)
    #                'spacing' : 100.0, # the space between two electrodes (micro meters)
    #                'offset_x' : 0.0, # the x axis offset from the center of the sheet (micro meters)
    #                'offset_y' : 0.0, # the y axis offset from the center of the sheet (micro meters)
    #              }
    #},

    #"5" : {
    #  'component' :  'mozaik.sheets.population_selector.SimilarAnnotationSelector',
    #  'variables' : ("spikes"),
    #  'params' :  {
    #                'num_of_cells' : 1000,
    #                'annotation' : 'LGNAfferentOrientation',
    #                'distance' : 0.2,
    #                'value': 0.0,
    #                'period' :  3.14159265359, 
    #              }
    #},

    ## CENTER
    #"6" : {
    #  'component' :  'mozaik.sheets.population_selector.RCGrid',
    #  'variables' : ("spikes", "v", "gsyn_exc", "gsyn_inh"),
    #  'params' :  {
    #    'size': 100.0,  # the size of the grid (it is assumed to be square) - it has to be factor of spacing (micro meters)
    #    'spacing' : 20.0, # the space between two electrodes (micro meters)
    #    'offset_x' : 0.0, # the x axis offset from the center of the sheet (micro meters)
    #    'offset_y' : 0.0, # the y axis offset from the center of the sheet (micro meters)
    #  }
    #},

    ## VSDI
    #"7" : {
    #  'component' :  'mozaik.sheets.population_selector.RCGrid',
    #  'variables' : ("spikes", "v"),
    #  'params' :  {
    #    'size': 3000.0,  # the size of the grid (it is assumed to be square) - it has to be factor of spacing (micro meters)
    #    'spacing' : 200.0, # the space between two electrodes (micro meters)
    #    'offset_x' : 0.0, # the x axis offset from the center of the sheet (micro meters)
    #    'offset_y' : 0.0, # the y axis offset from the center of the sheet (micro meters)
    #  }
    #},

    ## MANY
    #"1" : {
    #  'component' :  'mozaik.sheets.population_selector.RCGrid',
    #  'variables' : ("spikes", "v", "gsyn_exc", "gsyn_inh"),
    #  'params' :  {
    #    'size': 3000.0,  # the size of the grid (it is assumed to be square) - it has to be factor of spacing (micro meters)
    #    'spacing' : 200.0, # the space between two electrodes (micro meters)
    #    'offset_x' : 0.0, # the x axis offset from the center of the sheet (micro meters)
    #    'offset_y' : 0.0, # the y axis offset from the center of the sheet (micro meters)
    #  }
    #},

}
